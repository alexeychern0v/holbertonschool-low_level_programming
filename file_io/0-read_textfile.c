#include "main.h"

/**
 *
 *
 *
 *
 */

ssize_t read_textfile(const char *filename, size_t letters)
{
	int f;

	if (filename == NULL)
		return (0);
	f = fopen (filename, O_RDONLY);
	if (fd = -1)
		return (0);









}
