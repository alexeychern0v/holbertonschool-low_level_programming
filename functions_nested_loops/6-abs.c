#include "main.h"

/**
 * _abs - find the absolute value of int
 *
 *@k: number
 *
 * Return: 0 succes
 */
int _abs(int k)
{
	if (k > 0)
		return (k);
	else if (k < 0)
	{
		int abs_k;

		return (abs_k);
	}
	else
	{
		int zero_k;

		return (zero_k);
	}
	return (0);
}
